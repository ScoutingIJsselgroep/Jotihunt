// import { fromJS } from 'immutable';
// import { makeSelectAddHintContainerDomain } from '../selectors';

// const selector = makeSelectAddHintContainerDomain();

describe('makeSelectAddHintContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
