// import { fromJS } from 'immutable';
// import { makeSelectJotihuntWikiDomain } from '../selectors';

// const selector = makeSelectJotihuntWikiDomain();

describe('makeSelectJotihuntWikiDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
