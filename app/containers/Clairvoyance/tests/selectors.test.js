// import { fromJS } from 'immutable';
// import { makeSelectClairvoyanceDomain } from '../selectors';

// const selector = makeSelectClairvoyanceDomain();

describe('makeSelectClairvoyanceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
