// import { fromJS } from 'immutable';
// import { makeSelectHintListDomain } from '../selectors';

// const selector = makeSelectHintListDomain();

describe('makeSelectHintListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
