// import { fromJS } from 'immutable';
// import { makeSelectMassiveMapDomain } from '../selectors';

// const selector = makeSelectMassiveMapDomain();

describe('makeSelectMassiveMapDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
