// import { fromJS } from 'immutable';
// import { makeSelectGroupListDomain } from '../selectors';

// const selector = makeSelectGroupListDomain();

describe('makeSelectGroupListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
