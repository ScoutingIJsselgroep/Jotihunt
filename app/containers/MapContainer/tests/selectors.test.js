// import { fromJS } from 'immutable';
// import { makeSelectMapContainerDomain } from '../selectors';

// const selector = makeSelectMapContainerDomain();

describe('makeSelectMapContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
