// import { fromJS } from 'immutable';
// import { makeSelectAddHuntDomain } from '../selectors';

// const selector = makeSelectAddHuntDomain();

describe('makeSelectAddHuntDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
